import { StyleSheet } from 'react-native';

export const customInputStyles = StyleSheet.create({
    container: {
        marginBottom: 16
    },
    label: {
        color: '#555',
        fontSize: 14,
        fontWeight: 'bold',
        marginBottom: 8
    },
    input: {
        width: '100%',
        padding: 12,
        borderRadius: 8,
        borderWidth: 1,
        borderColor: '#ccc',
        fontSize: 14
    }
});